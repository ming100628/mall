<!DOCTYPE html>
<html>
  <head>
    <title>Hktvmall</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <!-- prettier-ignore -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <!-- prettier-ignore -->
    <%= current_user %>
    <%= yield %>
  </body>
</html>
